export interface Settings {
    className: string,
    centerMode?: boolean,
    infinite?: boolean,
    centerPadding?: string,
    slidesToShow?: number,
    slidesToScroll?: number,
    speed: number,
    autoplay: boolean,
    autoplaySpeed: number,
    dots: boolean,
    arrows: boolean
  };